【历史记录，非权威参考】
本文件记录旧版程序的参数初始值，部分参数名称和默认值已过时（如"头部Y偏移"已废弃）。
当前默认值权威来源：AGENTS.md §8 和 docs/config.md。

---

把这些参数，作为程序的初始值：


检测分辨率=640
采集帧率=60

使用CUDA直接捕获=开启
捕获方式=桌面复制API
显示预览窗口=开启
调试缩放=1.0

置信度阈值=0.40
NMS阀值=0.50
最大检测数=10

身体Y偏移(近似)=0.20
头部Y偏移(近似)=0.08

视角X=121
视角Y=90

固定首发延迟(ms)=0ms
随机开火延迟(ms)=0ms
固定开枪时长(ms)=500ms
开枪时长波动(ms)=20ms

面板透明度=100%（运行调节范围是70%-100%）
UI缩放=1.00
从捕获中隐藏面板=关闭

自瞄键=RightMouseButton
开枪键=LeftMouseButton
瞄准镜=RightMouseButton

退出=F12
暂停自瞄=None（不设置按钮）
重载配置=None（不设置按钮）
打开设置面板=Home













